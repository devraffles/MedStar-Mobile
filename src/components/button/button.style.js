import { colors, fontSizes } from "../../constants/theme.js";

export const styles = {

    btn: {
        width: "100%",
        backgroundColor: colors.green,
        borderRadius: 8,
        padding: 12
    },

    text: {
        color: colors.gray1,
        fontSize: fontSizes.md,
        textAlign: "center"
    }

}