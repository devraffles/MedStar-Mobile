import { COLORS, FONT_SIZE } from "../../constants/theme.js";

export const styles = {

    btn: {
        width: "100%",
        backgroundColor: COLORS.green,
        borderRadius: 8,
        padding: 12
    },

    text: {
        color: COLORS.gray1,
        fontSize: FONT_SIZE.md,
        textAlign: "center"
    }

}