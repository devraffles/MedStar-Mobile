import { colors, fontSizes } from "../../constants/theme.js";

export const styles = {
    container: {
        backgroundColor: colors.white,
        flex: 1,
        padding: 12,
    },
    text: {
        fontSize: fontSizes.md,
        color: colors.gray1,
        marginBottom: 15
    }
}